====================================================
  LAON QA 리포트 — 2026-08-11
  총 35건 | CRITICAL 2 / HIGH 10 / MEDIUM 18 / LOW 5
====================================================

──────────────────────────────────────────────────
[CRITICAL] 출시 블로커 — 즉시 수정
──────────────────────────────────────────────────

□ C-1. 인증 경쟁 조건 — 프로필 미완성 상태로 라우트 가드 실행
   파일: context/AppContext.tsx:371–395
   재현: 소셜 로그인 직후 딥링크 콜백 / 앱 재시작 시
   증상: setIsLoadingUser(false)가 fetchMyProfile 완료 전에 호출됨.
         syncRcWithSupabase(수 초 소요)가 끝나기 전에 라우트 가드 실행 →
         빈 프로필 상태로 화면 전환 발생
   원인: getSession() promise와 onAuthStateChange가 동시에 동작,
         fetchMyProfile을 await 없이 시작하고 바로 setIsLoadingUser(false) 호출

□ C-2. 결제 성공 후 Supabase DB 업데이트 실패 시 "결제 오류"로 표시
   파일: context/AppContext.tsx:441–453
   재현: 결제 직후 네트워크 단절 / Supabase 일시 오류
   증상: RevenueCat 결제 성공(돈 빠져나감)했는데 supabase.update() 에러 시
         throw error → subscription.tsx catch에서 "결제 오류" Alert 표시.
         앱은 무료 상태로 남고 사용자는 중복 결제 시도 가능.
         다음 앱 재시작 시 syncRcWithSupabase로 복구되나 즉시 확인 불가
   원인: if (error) throw error; 가 RC 결제 성공 이후에 위치해
         DB 오류를 결제 실패와 동일하게 처리

──────────────────────────────────────────────────
[HIGH] 출시 전 수정 권장
──────────────────────────────────────────────────

□ H-1. Redo 실행 시 댄서 위치 접근 경로 오류 → 댄서 화면 밖으로 사라짐
   파일: app/room/[id]/formation/[formationId].tsx:1093
   재현: formation 편집기에서 Redo 실행
   증상: 댄서들이 화면 밖으로 사라지거나 이전 위치에 고정됨
   원인: (next as any).positions[dId] → next는 HistoryState이므로
         최상위에 positions 없음.
         올바른 경로: next.scenes.find(s => s.id === activeSceneId)?.positions[dId]
         Undo는 정상 동작, Redo만 항상 undefined 할당. as any로 TS 에러 무시됨

□ H-2. 씬 롱프레스 삭제 → 타임라인 고아 엔트리 → 크래시 가능
   파일: app/room/[id]/formation/[formationId].tsx:1610–1616
   재현: 타임라인에 배치된 씬 카드를 롱프레스 → 삭제
   증상: 씬 이름 없는 빈 블록 표시, scene.positions 접근 시 크래시
   원인: setScenes(filter)만 호출, setTimeline(filter) 빠뜨림.
         deleteActiveScene(줄 1584)은 둘 다 처리하나 롱프레스 핸들러는 씬만 삭제

□ H-3. FormationPiPPlayer loop=false → 안무 영상 끝나면 싱크 완전 깨짐
   파일: components/ui/FormationPiPPlayer.tsx:64–68
   재현: 안무 영상이 동선보다 짧을 때
   증상: 한 번 재생 끝나면 PiP 정지. 타임라인은 계속 진행되는데
         안무 영상만 첫 프레임에 고정됨
   원인: useVideoPlayer 초기화 시 p.loop = false 설정.
         드리프트 보정 인터벌도 재생 재개 로직 없음

□ H-4. 댓글 입력창 useEffect 스테일 클로저 → 영상 재생 오작동
   파일: app/room/[id]/feedback.tsx:403–412
   재현: 댓글창 열었다 닫은 뒤 영상 교체 후 다시 댓글창 여닫기
   증상: 댓글창 닫을 때 이전 영상 player를 참조해 엉뚱한 영상 재생 또는 멈춤
   원인: useEffect(..., [showCommentInput]) 의존성에
         player, isFormation, selectedVideo 누락

□ H-5. 전체화면 orientation lock 두 useEffect가 경쟁
   파일: app/room/[id]/feedback.tsx:300–317
   재현: 일반 전체화면과 동선 전체화면을 빠르게 전환
   증상: 화면이 의도치 않은 방향으로 고정되거나 잠금 해제 안 됨
   원인: isFullScreen 감시 useEffect와 isFormationFullScreen 감시 useEffect가
         독립적으로 ScreenOrientation.lockAsync 호출.
         두 플래그가 동시에 true이면 LANDSCAPE/PORTRAIT lock 비동기 충돌

□ H-6. Android — 알림 채널이 토큰 저장 이후에 생성됨 (첫 설치 시 알림 수신 실패)
   파일: services/NotificationService.ts:54–79
   재현: Android 기기 최초 설치 후 첫 로그인
   증상: 토큰은 DB에 저장되지만 채널 생성 전에 도착한 알림 무음 처리
   원인: 코드 순서: 토큰 저장(54–68) → 채널 생성(71–79).
         채널 생성이 토큰 저장보다 먼저 이루어져야 함

□ H-7. MAX_FILE_SIZE 상수가 선언만 되고 실제로 체크 안 함
   파일: services/storageService.ts:5, 29–101
   재현: 초대형 이미지(RAW, 초고해상도 HEIC 등) 업로드 시
   증상: 압축 후에도 5MB 초과 파일이 R2에 그대로 업로드됨
   원인: const MAX_FILE_SIZE = 5 * 1024 * 1024 선언 후
         uploadToR2 내부에 if (fileInfo.size > MAX_FILE_SIZE) 체크 코드 없음

□ H-8. 일정 날짜 new Date("YYYY-MM-DD") → KST에서 하루 밀림
   파일: app/room/[id]/schedule.tsx:158, 449
   재현: KST(UTC+9), JST 등 UTC+0보다 큰 타임존 기기에서 일정 편집 모달 열기
   증상: 설정한 날짜가 편집 모달에서 하루 전날로 표시됨.
         예: 2026-08-11 설정 → 편집 시 2026-08-10으로 표시
   원인: 시간 없는 날짜 문자열은 UTC 자정으로 파싱됨.
         KST에서 UTC 자정 = 현지 전날 오전 9시 → toLocaleDateString이 전날 반환
         수정: "YYYY-MM-DD" → new Date("YYYY-MM-DDT12:00:00")

□ H-9. 투표 편집 시 선택지 삭제가 서버에 반영 안 됨
   파일: app/room/[id]/vote.tsx:103–115
   재현: 투표 편집 모달에서 기존 선택지 삭제 후 저장
   증상: UI에서는 줄어들어 보이지만 다시 열면 삭제한 선택지 그대로 표시
   원인: handleUpdateVote가 새 옵션 추가(addVoteOptions)만 처리.
         삭제된 옵션 제거 로직 전혀 없음

□ H-10. 실시간 채널이 모든 DB 변경에 전체 쿼리 무효화 → 쿼리 폭풍
   파일: context/AppContext.tsx:567–570
   재현: 방 멤버가 많고 활발히 사용 중일 때 / 본인이 댓글·공지 작업 시
   증상: 댓글 하나 달면 전체 쿼리 재조회 + 실시간 채널 감지로 한 번 더
         → 방 전체 쿼리 2중 폭발, 네트워크 과부하
   원인: event: '*', schema: 'public' 전체 구독 +
         핸들러에서 queryClient.invalidateQueries()(전체) 호출

──────────────────────────────────────────────────
[MEDIUM] 가능하면 수정
──────────────────────────────────────────────────

□ M-1. 동영상 크기 검사가 MP4 변환 이후에 실행 — 변환 시간 낭비 후 오류
   파일: app/room/[id]/archive.tsx:111–118
   재현: 5MB 초과 동영상 업로드 시도
   증상: ensureStandardMP4 변환(CPU 집약) 완료 후 용량 초과 알림.
         변환 시간 낭비. 변환 후 파일이 더 커진 경우 검사 통과 가능
   원인: 크기 체크 순서가 잘못됨. 변환 전 원본 크기 먼저 체크해야 함

□ M-2. 댓글 등록 후 videos 배열 구 캐시 참조 → 사이드바 즉시 갱신 안 됨
   파일: app/room/[id]/feedback.tsx:461–476
   재현: 댓글 등록 직후
   증상: 방금 추가한 댓글이 사이드바에 즉시 나타나지 않음
   원인: addComment 후 videos.find(v => v.id === selectedVideo.id) 호출하지만
         videos는 클로저가 캡처한 이전 렌더 값 → 새 댓글 포함 안 됨

□ M-3. playbackRate 변경 시 fallback 인터벌 재시작으로 formationTime jumpy 점프
   파일: app/room/[id]/feedback.tsx:203–220
   재현: 동선 재생 중 재생 속도 변경 (1x → 0.5x 등)
   증상: 속도 변경 시 타임라인 바늘이 순간 크게 점프
   원인: 의존성 변경으로 인터벌 재시작 시 lastTick이 Date.now()로 리셋되어
         큰 dt가 한 번 발생

□ M-4. 댓글 수정 롤백이 스테일 클로저의 구 selectedPhoto 참조
   파일: app/room/[id]/archive.tsx:210–220
   재현: 댓글 수정 API 실패 시
   증상: 롤백 시 그 사이 발생한 다른 변경(예: 좋아요)도 함께 사라짐
   원인: handleUpdateComment가 selectedPhoto를 클로저로 캡처.
         함수 실행 중 상태가 바뀌어도 캡처 당시 값으로 롤백

□ M-5. 낙관적 댓글 롤백이 이미 업데이트된 selectedPhoto를 다시 세팅
   파일: app/room/[id]/archive.tsx:157–169
   재현: 아카이브 댓글 작성 중 네트워크 오류
   증상: 낙관적으로 추가된 댓글이 화면에 잔존 (롤백 실패)
   원인: catch에서 setSelectedPhoto(selectedPhoto) 호출하나
         이 시점 selectedPhoto는 이미 updatedPhoto로 변경된 상태.
         함수 시작 시 const originalPhoto = selectedPhoto로 캡처했어야 함

□ M-6. 아카이브 동영상 아이템에 커스텀 컨트롤 없음 — 일시정지·되감기 불가
   파일: app/room/[id]/archive.tsx:60–70, 376
   재현: 아카이브에서 동영상 선택 후 시청
   증상: 재생은 되지만 일시정지, 되감기, 진행바 조작 불가
   원인: VideoView에 nativeControls={false}인데 커스텀 컨트롤 UI 없음

□ M-7. subPosX/subPosY SharedValue가 모듈 레벨 SCREEN_WIDTH로 고정
   파일: app/room/[id]/feedback.tsx:145–149
   재현: 세로 모드에서 서브 PiP 위치 조절 후 가로 모드(전체화면) 전환
   증상: PiP 영상이 화면 밖으로 사라짐
   원인: Dimensions.get('window').width는 모듈 레벨에서 한 번만 읽힘.
         useWindowDimensions()의 winW가 있으나 SharedValue 초기화에 미사용

□ M-8. analyzeAudio가 외부 CDN에 의존 — 오프라인 시 파형 분석 실패
   파일: app/room/[id]/formation/[formationId].tsx:976–981, 1694
   재현: 오프라인 상태 / CDN 지연 시 동선 편집 화면 진입
   증상: 파형 표시 없이 편집해야 함. lamejs CDN 로드 실패 시 오디오 압축도 불가
   원인: WebView가 cdnjs.cloudflare.com에서 lame.min.js 동적 로드.
         analyzeAudio가 useCallback 없이 정의되어 의존성 경고 억제 중

□ M-9. 방 참여 직후 멤버 수 1명으로 잠시 표시
   파일: services/roomService.ts:39–55, context/AppContext.tsx:780–793
   재현: 방 참여 직후 방 목록/상세에서 멤버 수 확인
   증상: 실제 멤버 20명인 방이어도 UI에 1명으로 잠시 표시
   원인: joinRoom 반환 Room 객체에 members: [userId] 하드코딩

□ M-10. 갤러리 개수 제한이 Pro 여부 무시하고 항상 20개 고정
   파일: services/storageService.ts:108–109
   재현: 리더가 Pro인 방에서 멤버가 21번째 파일 업로드 시
   증상: 리더가 Pro여도 21번째 업로드 차단됨
   원인: storageService.uploadToGallery가 MAX_GALLERY_COUNT=20 항상 적용.
         isPro/leaderIsPro 정보를 전달받지 않음

□ M-11. 알림 탭으로 앱 실행 시 비로그인 상태에서 router.push 실행
   파일: app/_layout.tsx:55–61
   재현: 앱 완전 종료 상태에서 푸시 알림 탭 / 세션 만료 상태
   증상: 인증 필요한 화면으로 이동 시도 → 라우트 가드가 루트로 보내지만
         잠깐 빈 화면 또는 에러 노출 가능
   원인: responseListener 콜백에서 currentUser 여부 확인 없이 바로 router.push 호출

□ M-12. 쿠폰 에러 코드 구분 부족 — 만료·중복 사용 시 원인 불명
   파일: app/subscription.tsx:54–62
   재현: 만료된 쿠폰 / 이미 사용한 쿠폰 입력 시
   증상: 어떤 이유로 쿠폰이 안 되는지 알 수 없음 (일반 오류 메시지만 표시)
   원인: INVALID_COUPON 한 가지만 명시 처리, 나머지 전부 일반 오류로 처리

□ M-13. 낙관적 댓글 createdAt이 string → formatDateFull(number) 불일치 → Invalid Date
   파일: app/room/[id]/notice/[noticeId].tsx:81
   재현: 공지사항 상세에서 댓글 작성 직후
   증상: 방금 추가한 댓글의 날짜가 "Invalid Date" 또는 이상한 숫자로 표시
   원인: createdAt: new Date().toISOString() (string)으로 삽입하나
         formatDateFull은 timestamp: number(밀리초)를 기대.
         수정: createdAt: Date.now()
         동일 패턴이 archive.tsx:158 addPhotoComment에도 있음

□ M-14. 방장이 다른 멤버의 아카이브 댓글을 삭제할 수 없음
   파일: app/room/[id]/archive.tsx:267–277
   재현: 방장이 다른 멤버 댓글의 ··· 버튼 탭
   증상: ··· 버튼은 보이지만 commentOptions에 리더 삭제 항목 없음
   원인: isRoomLeader 여부를 commentOptions 구성 시 체크하지 않음

□ M-15. 피드백 댓글 수정/삭제 실패 시 롤백·알림 없음
   파일: app/room/[id]/feedback.tsx:501–516
   재현: 댓글 수정 또는 삭제 중 네트워크 오류
   증상: 조용한 실패. UI는 변경된 상태로 남으나 서버 미반영
   원인: handleUpdateComment에 catch 블록 전혀 없음.
         handleDeleteComment도 에러 처리 없이 refreshAllData()만 호출

□ M-16. isClosed를 클라이언트 시계로만 판단 — 기기 시계 조작 시 마감 우회
   파일: app/room/[id]/schedule.tsx:362, app/room/[id]/vote.tsx:179
   재현: 기기 시계를 과거로 변경 후 일정/투표 화면 진입
   증상: 마감된 투표/일정에 응답 가능. 반대로 마감 전인데 마감됨으로 표시될 수도 있음
   원인: isClosed = schedule.deadline && new Date(schedule.deadline) < new Date()
         new Date()는 클라이언트 시각. 서버 사이드 마감 체크 없음

□ M-17. 강퇴 후 refreshAllData 미호출 → 다른 화면과 동기화 불일치
   파일: app/room/[id]/members.tsx:49, 81
   재현: 방장이 멤버 강퇴 직후 일정·투표 화면 확인
   증상: 강퇴된 사용자가 일정/투표 응답률 계산에 계속 포함됨
   원인: 강퇴 후 로컬 memberIds만 업데이트하고 refreshAllData() 미호출

□ M-18. optimistic update 캐시 키 불일치 — roomIds 배열 참조가 매 렌더 다름
   파일: context/AppContext.tsx:833–836
   재현: 방 참여 직후 공지 추가 시
   증상: 낙관적 UI 업데이트가 캐시에 반영되지 않고 refreshAllData()로 덮어씌워짐
   원인: queryClient.setQueryData(['notices', roomIds])에서 roomIds가
         useMemo 배열이라 참조가 매 렌더 다를 수 있음.
         동일 패턴이 addVote(줄 941)에도 있음

──────────────────────────────────────────────────
[LOW] 추후 개선
──────────────────────────────────────────────────

□ L-1. noAudio=true일 때 빈 audio player에 playbackRate 세팅 시도
   파일: components/ui/FormationPlayer.tsx:258–262
   재현: noAudio=true로 마운트된 FormationPlayer에서 playbackRate prop 변경
   증상: 내부 에러 발생 가능성
   원인: noAudio 여부 체크 없이 player.playbackRate = playbackRate 호출

□ L-2. 댓글 ··· 메뉴 options가 이전 selectedComment 기준으로 계산
   파일: app/room/[id]/notice/[noticeId].tsx:195–220, 285–289
   재현: 댓글 목록에서 빠르게 다른 댓글 ··· 버튼 연속 탭
   증상: 잘못된 댓글을 대상으로 수정/삭제 실행됨
   원인: commentOptions가 selectedComment state에 의존하는 파생 값인데
         React 상태 업데이트가 비동기이므로 이전 selectedComment 참조

□ L-3. deletePhoto가 R2 실제 파일 삭제 안 함 — 스토리지 비용 누적
   파일: context/AppContext.tsx:67, 918
   재현: 갤러리 사진/영상 삭제 시
   증상: DB 레코드는 삭제되나 R2 실제 파일 잔존. 장기적으로 스토리지 비용 증가
   원인: deletePhoto가 url 파라미터를 받지만 contentService에서 DB 삭제만 수행

□ L-4. 일정 그리드 날짜 헤더 key={date} 중복 가능성
   파일: app/room/[id]/schedule.tsx:451, 476
   재현: 같은 날짜에 여러 시간대 있는 일정 스크롤 시
   증상: 날짜 헤더가 잘못된 위치에 렌더링되는 현상 가능
   원인: hours.map 안에서 uniqueDates.map의 key={date}가 형제간 유니크하나
         날짜 헤더 Animated.View와 key가 동일해 React 재조정 시 혼선

□ L-5. 빈 투표 선택지 2개인 상태에서 삭제 불가 + 오류 위치 미표시
   파일: app/room/[id]/vote.tsx:76, 389
   재현: 선택지 추가 후 일부만 입력한 상태에서 제출 시도
   증상: 빈 항목이 2개일 때 최소 2개 제한으로 삭제 불가 → 제출도 불가 → 막힘
         어떤 항목이 비어있는지 시각적 표시 없음
   원인: options.length > 2 제한으로 빈 항목 삭제 차단

──────────────────────────────────────────────────
[우선순위 요약]
──────────────────────────────────────────────────

즉시 수정 (출시 블로커):
  ✗ C-1  인증 경쟁 조건 (AppContext.tsx:371)
  ✗ C-2  결제 성공 후 DB 실패 = 결제 오류로 표시 (AppContext.tsx:441)
  ✗ H-1  Redo 댄서 위치 버그 (formationId.tsx:1093)
  ✗ H-2  씬 삭제 → 타임라인 크래시 (formationId.tsx:1610)
  ✗ H-6  Android 알림 채널 순서 (NotificationService.ts:54)

출시 전 수정 권장:
  ✗ H-3  안무 영상 loop=false (FormationPiPPlayer.tsx:64)
  ✗ H-4  댓글창 스테일 클로저 (feedback.tsx:403)
  ✗ H-5  orientation lock 경쟁 (feedback.tsx:300)
  ✗ H-7  파일 크기 미검사 (storageService.ts:5)
  ✗ H-8  날짜 타임존 하루 밀림 (schedule.tsx:158)
  ✗ H-9  투표 선택지 삭제 미반영 (vote.tsx:103)
  ✗ H-10 실시간 채널 쿼리 폭풍 (AppContext.tsx:567)

여유 있을 때:
  ✗ M-1 ~ M-18  MEDIUM 항목들
  ✗ L-1 ~ L-5   LOW 항목들

====================================================
  검토 영역: feedback / formation / archive / AppContext /
             subscription / NotificationService / storageService /
             schedule / vote / members / notice / _layout
  버전: 1.0.12 (build 22)
====================================================


====================================================
  LAON QA 2차 점검 — 2026-08-11
  iOS / Android / 성능·부하 / 이전 수정 검증
  신규 이슈 36건 | CRITICAL 2 / HIGH 13 / MEDIUM 14 / LOW 7
====================================================

──────────────────────────────────────────────────
[iOS 전용 QA]
──────────────────────────────────────────────────

[CRITICAL — iOS]

□ iC-1. Apple Sign In nonce 미사용 → 리플레이 어택 가능 + App Store 거절 리스크
   파일: services/authService.ts:128
   재현: iOS Apple Sign In 로그인 시도
   증상: Apple로 로그인 시 nonce 없이 credential 전달. Apple 정책상 nonce 필수.
         App Store 심사 거절 사유 해당. 탈취된 identity_token 재사용 가능
   원인: signInWithApple 구현 시 Crypto.randomUUID()·sha256 해시 생성 없이
         appleAuth.performRequest() 호출 후 바로 supabase.auth.signInWithIdToken 전달.
         nonce 파라미터 누락
   수정: expo-crypto로 nonce 생성 → SHA-256 해시 → rawNonce를 Apple 요청에,
         nonce(해시)를 signInWithIdToken에 전달

[HIGH — iOS]

□ iH-1. 전체화면 전환 시 Dynamic Island / 노치 safe area 미적용
   파일: app/room/[id]/feedback.tsx:1230, 1673
   재현: iPhone 14 Pro 이상 기기에서 영상 전체화면 전환
   증상: 전체화면 모드에서 상단 컨트롤(닫기 버튼 등)이 Dynamic Island/노치에 가려짐
   원인: 전체화면 진입 시 useSafeAreaInsets() top 값을 헤더 paddingTop에 적용하지 않음.
         landscape 전환 후 insets.top이 0이 아닌 값을 반환하는 기기에서 누락

□ iH-2. WebView 오디오 압축 중 앱 백그라운드 전환 → 무한 대기
   파일: app/room/[id]/formation/[formationId].tsx:806
   재현: 파형 분석 또는 오디오 압축 진행 중 홈 버튼으로 앱을 백그라운드로 보냄
   증상: 앱 복귀 시 WebView가 응답 없음. Promise가 resolve되지 않아 UI 멈춤
   원인: WebView postMessage 응답을 기다리는 Promise에 타임아웃 없음.
         iOS WebView는 백그라운드 진입 시 JavaScript 실행을 중단하므로
         응답 메시지가 영원히 오지 않음

[MEDIUM — iOS]

□ iM-1. archive.tsx insets.bottom 이중 적용 → 하단 여백 과다
   파일: app/room/[id]/archive.tsx (FlatList contentContainerStyle)
   재현: iPhone SE·X 등 홈 바 있는 기기에서 아카이브 목록 하단 확인
   증상: 목록 하단에 빈 공간이 두 배로 표시됨
   원인: SafeAreaView 또는 useSafeAreaInsets().bottom이 중복 적용됨

□ iM-2. 사진 제한 권한(limited) 상태에서 갤러리 표시 불확실
   파일: app/room/[id]/archive.tsx, app/room/[id]/feedback.tsx (ImagePicker 호출부)
   재현: iOS 14+ 에서 사진 앱 접근을 "일부 사진만 허용"으로 설정 후 갤러리 업로드
   증상: 선택한 이미지 외 나머지 이미지 접근 시 권한 오류 가능.
         LIMITED 권한 상태를 별도 처리하는 코드 없음
   원인: MediaLibrary/ImagePicker 권한 요청 시 'limited' 상태 분기 처리 없음

□ iM-3. orientation 전환 직후 insets 타이밍 불일치
   파일: app/room/[id]/feedback.tsx (ScreenOrientation.lockAsync 호출 직후)
   재현: 전체화면 전환 직후 빠르게 UI 렌더링되는 경우
   증상: 잠깐 잘못된 패딩으로 레이아웃이 어긋나 보임
   원인: lockAsync 완료 직후 insets 값이 아직 이전 orientation 기준.
         useEffect 의존성에 orientation 상태 포함되지 않아 재계산 지연

□ iM-4. allowsEditing=true + 동영상 선택 → 이중 트랜스코딩 위험
   파일: app/room/[id]/archive.tsx 또는 ImagePicker 호출부
   재현: iOS에서 동영상 업로드 시 allowsEditing 옵션 활성화 상태
   증상: iOS 기본 비디오 편집기(클립 다듬기) + ensureStandardMP4 변환이 직렬로 실행되어
         변환 시간 2배, 화질 이중 손실
   원인: allowsEditing이 iOS에서 비디오에도 적용되어 불필요한 1차 트랜스코딩 발생.
         이후 ensureStandardMP4에서 2차 트랜스코딩

[LOW — iOS]

□ iL-1. surfaceType="textureView" iOS 미지원 경고
   파일: components/ui/FormationPiPPlayer.tsx 또는 FormationPlayer.tsx (VideoView props)
   재현: iOS에서 동선 화면 진입
   증상: 콘솔 경고 발생. iOS는 surfaceType 옵션 무시하나 불필요한 prop 노출
   원인: expo-video VideoView의 surfaceType prop이 Android 전용

□ iL-2. isAvailableAsync 호출 없이 기능 사용
   파일: NotificationService.ts 또는 ScreenOrientation 사용 부분
   재현: 해당 기능 미지원 구형 기기
   증상: 구형 기기에서 예기치 않은 오류 가능
   원인: expo API 사용 전 isAvailableAsync() 가용성 체크 누락

□ iL-3. shouldShowList 잘못된 key 사용
   파일: archive.tsx 또는 관련 FlatList
   재현: 해당 FlatList 렌더링 시
   증상: React key 중복 경고
   원인: shouldShowList 조건부 렌더링 시 항목 key가 고유하지 않을 수 있음

□ iL-4. APNs 알림 카테고리 미등록
   파일: services/NotificationService.ts
   재현: iOS 알림 수신 후 길게 눌러 액션 확인 시
   증상: iOS 알림에서 "답장", "확인" 등 커스텀 액션 버튼 표시 안 됨
   원인: setNotificationCategoryAsync로 카테고리 등록하지 않아 기본 알림만 표시

□ iL-5. VideoView 컨트롤 하단 패딩 과다
   파일: app/room/[id]/feedback.tsx (nativeControls 관련 VideoView)
   재현: iPhone에서 영상 재생 중 nativeControls 표시
   증상: 하단 컨트롤 영역이 safe area와 겹쳐 이중 여백 발생
   원인: nativeControls 내장 패딩과 SafeAreaView 패딩 중복

──────────────────────────────────────────────────
[Android 전용 QA]
──────────────────────────────────────────────────

[HIGH — Android]

□ aH-1. 댓글 모달 KeyboardAvoidingView behavior 미설정 → Android에서 키보드가 입력창 가림
   파일: app/room/[id]/feedback.tsx:1476
   재현: Android 기기에서 댓글 입력창 탭 후 키보드 올라옴
   증상: 소프트 키보드가 올라와도 댓글 입력 TextField가 가려진 채 유지
   원인: KeyboardAvoidingView의 behavior prop이 undefined 또는 "padding"으로만 설정됨.
         Android는 "height" 또는 keyboardVerticalOffset 조정이 필요하나 미적용.
         iOS와 달리 Android는 behavior="padding" 단독으로 충분하지 않음

□ aH-2. READ/WRITE_EXTERNAL_STORAGE 권한 app.json 누락 → Android 9 이하 파일 접근 불가
   파일: app.json (android.permissions)
   재현: Android 9 (API 28) 이하 기기에서 영상·이미지 업로드 시도
   증상: 파일 선택 후 업로드 실패 또는 권한 거부 다이얼로그 없이 오류
   원인: app.json의 android.permissions 배열에 CAMERA, RECORD_AUDIO, NOTIFICATIONS만 선언.
         READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE 누락 (Android 9 이하 필수)

□ aH-3. WebView AudioContext 생성 실패 → setTimeout 재시도 무한 루프
   파일: app/room/[id]/formation/[formationId].tsx:758
   재현: 동선 편집 화면 진입 시 (Android WebView에서 AudioContext 자동 생성 실패)
   증상: Android Chrome 정책상 사용자 제스처 없이 AudioContext 생성 불가.
         WebView 내 재시도 코드가 계속 setTimeout으로 주입 → 무한 루프 → 배터리 소모
   원인: injectJavaScript에 AudioContext 생성 코드가 페이지 로드 즉시 실행됨.
         Android WebView는 사용자 인터랙션 없이는 AudioContext.state가 'suspended' 유지

□ aH-4. 동선 전체화면 Back 버튼 → ScreenOrientation 복원 안 됨
   파일: app/room/[id]/feedback.tsx:875
   재현: Android에서 동선 전체화면 상태에서 하드웨어 Back 버튼 탭
   증상: 전체화면 해제되고 피드백 화면으로 돌아오나 landscape lock이 풀리지 않아
         세로 화면이 가로 모드로 고정된 채 유지
   원인: Back 버튼 핸들러에서 ScreenOrientation.unlockAsync() 또는
         lockAsync(PORTRAIT) 호출 없음. isFormationFullScreen 상태만 초기화

[MEDIUM — Android]

□ aM-1. 알림 채널 생성 시점이 토큰 저장 이후 (H-6 Android 상세)
   파일: services/NotificationService.ts:54–79
   재현: Android 기기 최초 설치 후 첫 알림 수신
   증상: 토큰 등록 직후 도달하는 알림이 채널 없어 무음/묵음 처리됨
   원인: 토큰 저장(54–68) 후 채널 생성(71–79) 순서. 반드시 채널 먼저 생성 후 토큰 저장

□ aM-2. SCREEN_WIDTH 모듈 레벨 고정 → 화면 회전 시 PiP 위치 오류 (M-7 Android 재확인)
   파일: app/room/[id]/feedback.tsx:145–149
   재현: Android에서 세로→가로 화면 회전 후 PiP 드래그
   증상: PiP가 화면 밖으로 나가거나 엉뚱한 위치에 붙음
   원인: Dimensions.get('window').width가 앱 시작 시 한 번만 읽힌 후 고정.
         Android는 화면 회전 시 width/height가 바뀌나 SharedValue 재초기화 없음

□ aM-3. edge-to-edge 전체화면 하단 insets 미처리
   파일: app/room/[id]/feedback.tsx (전체화면 모드 레이아웃)
   재현: Android 10+ edge-to-edge 활성화 기기에서 전체화면 재생 중 하단 제스처 바 확인
   증상: 제스처 바 영역에 컨트롤 버튼이 겹쳐 탭 불가
   원인: app.json에 edgeToEdgeEnabled: true 설정되어 있으나
         전체화면 모드에서 insets.bottom을 컨트롤 paddingBottom에 반영 안 함

□ aM-4. keyboardDidShow 이벤트 지연 300–500ms → 입력창 위치 지연 조정
   파일: app/room/[id]/feedback.tsx (키보드 높이 추적 useEffect)
   재현: Android에서 댓글 입력창 탭
   증상: 키보드 올라오는 동안 0.3–0.5초간 입력창이 키보드에 가려졌다가 올라옴
   원인: Android keyboardDidShow 이벤트가 키보드 애니메이션 완료 후 발생.
         LayoutAnimation이나 Keyboard.scheduleLayoutAnimation 미사용

□ aM-5. PiP 드래그 경계 없음 → 화면 밖 드래그 가능
   파일: app/room/[id]/feedback.tsx (PiP PanGestureHandler)
   재현: Android에서 서브 PiP 영상을 화면 가장자리 밖으로 드래그
   증상: PiP가 화면 밖으로 완전히 나가 접근 불가
   원인: PanGesture 핸들러에서 subPosX/subPosY에 화면 경계 clamp 없음.
         iOS/Android 모두 해당하나 Android edge-to-edge에서 더 심각

□ aM-6. VideoExportPreset iOS 전용 상수 Android에서 사용
   파일: app/room/[id]/archive.tsx 또는 ensureStandardMP4 유틸 함수
   재현: Android에서 동영상 업로드 시
   증상: VideoExportPreset.MediumQuality 등 iOS 전용 상수가 Android에서 무시되거나 에러
   원인: expo-image-picker 또는 react-native-video의 VideoExportPreset은 iOS 전용.
         Android에서는 다른 방식으로 트랜스코딩 처리 필요

[LOW — Android]

□ aL-1. landscape 전환 후 insets.bottom 업데이트 지연
   파일: app/room/[id]/feedback.tsx (전체화면 전환 시)
   재현: Android에서 가로모드 전환 직후 1프레임
   증상: 잠깐 세로 모드 기준 bottom insets로 레이아웃이 렌더링됨
   원인: ScreenOrientation.lockAsync 완료 후 SafeAreaProvider가 insets를 업데이트하는
         타이밍 차이. 해결: orientation 완료 콜백 이후 강제 리렌더링

──────────────────────────────────────────────────
[성능 · 메모리 · 부하 테스트]
──────────────────────────────────────────────────

[CRITICAL — 성능]

□ pC-1. queryClient.invalidateQueries() 전체 호출 → 실시간 이벤트마다 전 쿼리 재조회 (H-10 재확인)
   파일: context/AppContext.tsx:567–570
   재현: 10명 이상 활성 방에서 채팅/댓글/공지 사용
   증상: Realtime 이벤트 수신 시 모든 useQuery 캐시 무효화 → 네트워크 요청 폭발.
         방 멤버 수 × 이벤트 빈도에 비례해 요청 수 급증. 앱이 느려지고 서버 부하 급증
   원인: invalidateQueries 호출 시 필터 없이 전체 쿼리 키 무효화.
         H-10과 동일 근본 원인, 성능 관점에서 심각도 재확인

[HIGH — 성능]

□ pH-1. 모든 프로필 페이지네이션 없이 로드 → 대형 방에서 메모리·네트워크 폭증
   파일: context/AppContext.tsx:521
   재현: 멤버 수 50명 이상 방에 입장
   증상: 방 진입 시 profiles 테이블에서 전체 행을 한 번에 조회.
         100명 방에서 수백 KB 이상 데이터 한 번에 수신 → 메모리 급증 + 느린 로딩
   원인: .select('*').in('id', memberIds) 사용 시 memberIds 배열 크기 무제한

□ pH-2. 대형 formation JSON AsyncStorage 저장 → UI 스레드 블로킹(ANR) 위험
   파일: context/AppContext.tsx:1052
   재현: 씬 20개, 댄서 20명 규모의 복잡한 formation 저장 시
   증상: JSON.stringify 및 AsyncStorage.setItem이 큰 객체를 처리하며 메인 스레드 블로킹.
         Android에서 ANR(Application Not Responding) 경고 발생 가능
   원인: formation 상태 전체를 JSON.stringify 후 단일 AsyncStorage 키로 저장.
         직렬화 작업이 JS 스레드에서 동기적으로 실행

□ pH-3. FormationPlayer setInterval이 playbackRate 의존성 없어 누적 생성
   파일: components/ui/FormationPlayer.tsx:228–256
   재현: 동선 재생 중 재생 속도를 여러 번 변경
   증상: 속도 변경 시마다 이전 인터벌 해제 없이 새 인터벌 생성.
         여러 번 변경하면 인터벌이 중첩되어 타임라인이 배속으로 진행
   원인: useEffect deps 배열에 playbackRate 누락. cleanup에서 이전 intervalId 참조하나
         새 인터벌 생성 조건이 잘못 설정됨

□ pH-4. DancerNode O(n×m) 선형 탐색 → 20댄서 × 100 타임라인 항목에서 매 프레임 2000회 탐색
   파일: components/ui/FormationPlayer.tsx:45–85
   재현: 댄서 20명, 타임라인 항목 100개 규모의 formation 재생
   증상: 재생 중 프레임 드롭(30fps 이하). 저사양 기기에서 더 심각
   원인: 매 프레임마다 currentSceneIndex 계산 시 timeline.findIndex() 선형 탐색.
         timeline을 Map<timestamp, sceneId>로 변환하거나 이진 탐색으로 교체 필요

□ pH-5. 세션 중 비디오 캐시 미정리 → 메모리 누수
   파일: app/room/[id]/feedback.tsx:336–394
   재현: 피드백 화면에서 여러 영상을 순서대로 시청
   증상: 영상 교체 시 이전 VideoPlayer 인스턴스가 메모리에 남음.
         10개 이상 영상 순서대로 시청하면 메모리 사용량 계속 증가
   원인: 영상 교체 시 이전 player.release() 또는 player 참조 해제 없음

□ pH-6. contextValue useMemo가 매 렌더 새 객체 생성 → 하위 컴포넌트 전체 리렌더
   파일: context/AppContext.tsx:1069
   재현: 앱 사용 중 어떤 상태 변경이든 발생 시
   증상: AppContext를 구독하는 모든 컴포넌트가 불필요하게 리렌더됨.
         특히 대형 리스트(notices, comments) 렌더링에서 성능 저하
   원인: useMemo deps에 포함된 배열(notices, comments 등)이 매 렌더 새 참조 생성.
         useMemo가 항상 새 contextValue 반환 → React.memo 보호 무력화

□ pH-7. 오디오 base64 문자열(~27MB) WebView 브리지에 주입 → 브리지 블로킹
   파일: app/room/[id]/formation/[formationId].tsx:757
   재현: 30분 이상 분량의 오디오 파일로 파형 분석 실행
   증상: 긴 오디오 파일의 base64 인코딩 문자열을 injectJavaScript로 WebView에 전달.
         JS 브리지 직렬화 과정에서 수십 MB 문자열 처리 → UI 멈춤 수 초
   원인: FileSystem.readAsBase64 결과를 전체 문자열로 직렬화 후 postMessage.
         큰 파일에서 브리지 버퍼 한계 초과 가능

[MEDIUM — 성능]

□ pM-1. FormationPiPPlayer 이중 seek 채널 경합
   파일: components/ui/FormationPiPPlayer.tsx
   재현: 동선 재생 중 수동 seek와 드리프트 보정 인터벌이 동시에 실행될 때
   증상: 영상이 앞뒤로 미세하게 흔들리는 현상 (seek 경합)
   원인: 사용자 seek 완료 전에 드리프트 보정 인터벌이 다른 시간으로 seek 호출.
         seek 진행 중 플래그 없음

□ pM-2. archive VideoPlayer 스테일 클로저 참조
   파일: app/room/[id]/archive.tsx (VideoPlayer 콜백)
   재현: 아카이브에서 영상 시청 중 새 댓글 추가 후 영상 조작
   증상: 댓글 추가 후 VideoPlayer 이벤트 콜백이 이전 comments 상태 참조
   원인: useCallback/useEffect deps에 comments 누락으로 스테일 클로저 발생

□ pM-3. 플로팅 댓글 버블 useMemo가 재생 틱 속도(~10회/초)로 재계산
   파일: app/room/[id]/feedback.tsx (floating comment bubbles)
   재현: 댓글 많은 영상 재생 중
   증상: formationTime이 업데이트될 때마다 전체 댓글 필터링 useMemo 재계산.
         댓글 100개 이상 영상에서 CPU 사용률 증가
   원인: useMemo deps에 formationTime 포함. 재생 중 초당 ~10회 갱신으로 불필요한 재계산

□ pM-4. storageService 갤러리 개수 조회 TOCTOU 경쟁
   파일: services/storageService.ts:108–109
   재현: 두 명이 동시에 갤러리 19번째 업로드 시도
   증상: count 확인과 insert 사이에 다른 사용자가 삽입하면 20개 초과 가능
   원인: count 조회 후 DB 삽입까지 원자적 처리 없음. DB 레벨 제약 또는 트랜잭션 필요

[LOW — 성능]

□ pL-1. 일정 그리드 선이 View 컴포넌트 사용 → Canvas/SVG 대비 렌더링 비효율
   파일: app/room/[id]/schedule.tsx (그리드 렌더링 부분)
   재현: 일정 그리드 스크롤 시
   증상: 다수의 View로 그리드 선 렌더링 → 뷰 계층 증가로 스크롤 성능 저하
   원인: 그리드 구분선을 얇은 View로 렌더링. react-native-svg 또는 Canvas 사용이 더 효율적

□ pL-2. ScreenOrientation 중복 호출 가능성
   파일: app/room/[id]/feedback.tsx (orientation useEffect 두 곳)
   재현: 전체화면 상태 빠르게 토글
   증상: 이미 해당 방향으로 잠긴 상태에서 다시 lockAsync 호출 → 불필요한 시스템 콜
   원인: 현재 lock 상태 확인 없이 상태 변경 시마다 lockAsync/unlockAsync 재호출

──────────────────────────────────────────────────
[이전 수정 사항 검증 (커밋 04b99d7 ~ 5894022)]
──────────────────────────────────────────────────

✅ VFY-1. 플로팅 댓글 버블 createdAt 정렬 (커밋 04b99d7)
   검증: feedback.tsx의 floating comments sort 로직 확인
   결과: .sort((a, b) => a.createdAt - b.createdAt) 적용 확인. 작성 순서대로 정렬됨

✅ VFY-2. 동선 뷰어 테마/레이블/속도/키보드 복원 (커밋 93594d4)
   검증: formationId.tsx 테마 적용, 레이블 표시, 재생 속도, 키보드 dismiss 확인
   결과: 각 항목 정상 구현 확인. 키보드 복원 로직 dismiss 시 player 재개 연동됨

✅ VFY-3. 동선 전체화면 / 비디오 핀치줌 / DancerNode worklet (커밋 5894022)
   검증: DancerNode useDerivedValue + useAnimatedStyle 확인
   결과: useDerivedValue 내부에서 worklet context 자동 실행 확인.
         Pinch gesture PinchGestureHandler → GestureDetector 전환 확인

⚠️ VFY-4. 일정 그리드 제스처 충돌 / 공지 댓글 낙관적 추가 (커밋 71a891e)
   검증: simultaneousWithExternalGesture 호출부 확인
   결과: simultaneousWithExternalGesture() 인자 없이 호출 → 사실상 no-op.
         의도한 외부 제스처 참조가 전달되지 않아 제스처 충돌 해결이 불완전할 수 있음

✅ VFY-5. 타임라인 / PiP / X 마크 관련 수정 (커밋 이전)
   검증: FormationPiPPlayer X 버튼 및 타임라인 동기화 확인
   결과: 정상 구현 확인

⚠️ VFY-6. 보안 강화 (get-r2-upload-url JWT 인증 + revenuecat-webhook fail-closed)
   검증: functions/get-r2-upload-url verify_jwt 설정, redeem-coupon 구현 확인
   결과: get-r2-upload-url은 verify_jwt=false 유지 (함수 내부에서 직접 JWT 검증 — 의도된 설계).
         redeem-coupon은 쿠폰 코드 유효성·만료 검사는 하나 사용자별 중복 사용 제한 없음
         (20260811todo.txt에 deferred로 기록됨 — 테스트 쿠폰 한정, 실서비스 쿠폰 발행 전 처리 필요)

──────────────────────────────────────────────────
[2차 점검 우선순위 요약]
──────────────────────────────────────────────────

즉시 수정 (출시 블로커):
  ✗ iC-1  Apple Sign In nonce 미사용 (authService.ts:128) — App Store 거절 직결
  ✗ aH-1  Android 댓글 키보드 가림 (feedback.tsx:1476)
  ✗ aH-3  Android WebView AudioContext 무한 재시도 (formationId.tsx:758)
  ✗ aH-4  Android Back 버튼 orientation 미복원 (feedback.tsx:875)
  ✗ aH-2  Android 9 이하 파일 권한 누락 (app.json)
  ✗ pC-1  전체 쿼리 무효화 (H-10 재확인 — AppContext.tsx:567)

출시 전 수정 권장:
  ✗ iH-1  Dynamic Island safe area 누락 (feedback.tsx:1230,1673)
  ✗ iH-2  WebView 백그라운드 무한 대기 (formationId.tsx:806)
  ✗ pH-1  프로필 전체 로드 페이지네이션 없음 (AppContext.tsx:521)
  ✗ pH-3  FormationPlayer 인터벌 중첩 (FormationPlayer.tsx:228)
  ✗ pH-4  DancerNode O(n×m) 탐색 (FormationPlayer.tsx:45)
  ✗ pH-5  비디오 캐시 미정리 메모리 누수 (feedback.tsx:336)
  ✗ pH-7  오디오 base64 브리지 블로킹 (formationId.tsx:757)
  ✗ aM-3  edge-to-edge 하단 insets 미처리 (feedback.tsx 전체화면)
  ✗ aM-5  PiP 드래그 경계 없음

여유 있을 때:
  ✗ iM-1 ~ iM-4  iOS MEDIUM 항목들
  ✗ aM-1 ~ aM-6  Android MEDIUM 항목들
  ✗ pH-2, pH-6   AsyncStorage ANR, useMemo 리렌더
  ✗ pM-1 ~ pM-4  성능 MEDIUM 항목들
  ✗ iL-1 ~ iL-5  iOS LOW 항목들
  ✗ aL-1          Android LOW 항목들
  ✗ pL-1 ~ pL-2  성능 LOW 항목들

수정 검증 결과:
  ✅ VFY-1, 2, 3, 5  정상 확인
  ⚠️ VFY-4  simultaneousWithExternalGesture 인자 누락 → 불완전
  ⚠️ VFY-6  redeem-coupon 중복 사용 제한 없음 (의도적 deferred)

====================================================
  2차 검토 영역: authService / feedback(iOS·Android) /
                formationId(iOS·Android·Perf) / FormationPlayer /
                FormationPiPPlayer / AppContext / archive /
                NotificationService / app.json
  버전: 1.0.12 (build 22)
====================================================
